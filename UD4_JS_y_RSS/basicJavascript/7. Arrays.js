// Para hacer un array, podemos usar []

var array = [1, 2, 3, 4, 5]

// Para recorrer un array, podemos usar for

for (var i = 0; i < array.length; i++) {
    console.log(array[i])
}



//Para añadir un elemento a un array, podemos usar push

array.push(6)

//Para eliminar un elemento de un array, podemos usar pop

array.pop(3)
// el 3 es el indice del elemento que queremos eliminar

//Para ordenar un array, podemos usar sort

array.sort()

//Para invertir un array, podemos usar reverse

array.reverse()

//Para eliminar un elemento de un array, podemos usar splice

array.splice(0, 1)
// con el primer parametro indicamos el indice del elemento que queremos eliminar, y con el segundo, el numero de elementos que queremos eliminar



